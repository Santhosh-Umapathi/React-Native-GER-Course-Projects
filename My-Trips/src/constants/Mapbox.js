import { MAPBOX_API_KEY } from "@env";

export default MapBox = {
  url: "https://api.mapbox.com/",
  token: MAPBOX_API_KEY,
  marker: "pin-s+555555",
  zoom: 12,
  style: "streets-v11/",
  size: "400x400",
};
